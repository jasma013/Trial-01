# Loop through numbers from 1 to 6 (inclusive)
for i in range(1, 7):
    # Initialize the variable x with an asterisk
    x = "*"
    # Multiply the asterisk by the current loop index to get the correct number of asterisks
    x = x * i
    # Print the resulting string of asterisks
    print(f"{x}")
